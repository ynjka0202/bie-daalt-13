# Claude Instructions

## Build & Run Commands
- Install: `npm install`
- Start: `npm start`
- Test: `npm test`

## Code Conventions
- Variable Naming: `camelCase`
- API Design: RESTful principles
- Errors: Use consistent error handling middleware

## No-Go Zones
- Do not use complex ORMs if not needed.
- No hardcoded credentials in code.